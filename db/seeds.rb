# No seed data. The application uses only real data, entered through the app
# (connect GitHub -> select repos -> projects/allocations are created; worklogs
# are drafted and saved by real users). Intentionally empty.
#
# Run with: bin/rails db:seed  (no-op)
